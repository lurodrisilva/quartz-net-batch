apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "batch-scheduler.fullname" . }}-config
  labels:
    {{- include "batch-scheduler.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
